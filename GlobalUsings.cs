// Global usings shared across the entire project.
// WPF's implicit-usings set does not include System.IO or System.Windows.Input,
// so we declare them here once rather than repeating them in every file.

global using System.IO;
global using System.Windows.Input;
